stages:
  - build
  - test
  - deploy

variables:
  CARGO_HOME: ${CI_PROJECT_DIR}/.cargo
  RUST_BACKTRACE: "1"

before_script:
  - echo "Global before_script - executed before every job"
  - rustc --version
  - cargo --version
