listen_addr = "0.0.0.0"
listen_port = {{LISTEN_PORT}}

# 后端服务器地址（支持两种方式）
# 1. HTTP URL: backend_url = "http://127.0.0.1:8081"
# 2. Unix Socket: backend_socket = "/tmp/gitfox-backend.sock" (优先级高于 URL)
backend_socket = "{{BACKEND_SOCKET}}"
backend_url = "{{BACKEND_URL}}"

# ============================================
# gRPC 服务配置
# ============================================
# Auth gRPC 服务地址（主应用提供的认证服务）
# auth_grpc_address = "http://[::1]:50051"

# GitLayer gRPC 服务地址（Git 操作服务）
# gitlayer_address = "http://[::1]:50052"

# 是否使用 gRPC 进行权限认证（推荐启用）
# use_grpc_auth = true

# 是否使用 GitLayer 处理 Git HTTP 请求
# use_gitlayer = true

# 静态文件路径
frontend_dist_path = "{{FRONTEND_DIST_PATH}}"
webide_dist_path = "{{WEBIDE_DIST_PATH}}"
assets_path = "{{ASSETS_PATH}}"
git_repos_path = "{{GIT_REPOS_PATH}}"

# 启用请求日志
enable_request_logging = true

# 启用 CORS
enable_cors = true

# 最大上传文件大小
# Git push 操作可能需要较大的限制，建议至少 500MB (524288000) 或 1GB (1073741824)
# 这个值会被 gitfox-omnibus 自动设置为与后端一致的值
max_upload_size = {{MAX_UPLOAD_SIZE}}

# WebSocket 超时时间 (3600秒 = 1小时)
websocket_timeout = 3600

# 静态文件缓存控制头
static_cache_control = "public, max-age=31536000, immutable"
